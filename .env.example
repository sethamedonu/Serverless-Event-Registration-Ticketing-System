# Frontend — copy to .env and fill in after SAM deploy
VITE_API_URL=https://<api-gateway-id>.execute-api.<region>.amazonaws.com/prod
VITE_COGNITO_USER_POOL_ID=<from SAM output: UserPoolId>
VITE_COGNITO_CLIENT_ID=<from SAM output: UserPoolClientId>
VITE_AWS_REGION=<your-region>
